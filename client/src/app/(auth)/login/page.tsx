'use client';
import { FormEvent, useState } from 'react';
const API = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:5000';
export default function LoginPage() {
  const [email, setEmail] = useState('demo@cristal.page'); const [password, setPassword] = useState('demo123'); const [error, setError] = useState(''); const [loading, setLoading] = useState(false);
  async function submit(event: FormEvent) { event.preventDefault(); setLoading(true); setError(''); try { const response = await fetch(`${API}/api/auth/login`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email, password }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error || 'เข้าสู่ระบบไม่สำเร็จ'); localStorage.setItem('cristalToken', data.token); localStorage.setItem('cristalUserId', data.user.id); localStorage.setItem('cristalUser', JSON.stringify(data.user)); window.location.href = '/dashboard'; } catch (err) { setError(err instanceof Error ? err.message : 'เข้าสู่ระบบไม่สำเร็จ'); } finally { setLoading(false); } }
  return <main className="flex min-h-screen items-center justify-center bg-[#edf1fa] p-6"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white p-8 shadow-soft"><h1 className="text-3xl font-black text-violet">Welcome to Cristal</h1><p className="mb-6 mt-2 text-slate-500">เข้าสู่ระบบเพื่อสร้างเว็บไซต์ด้วย AI</p><input required value={email} onChange={(e) => setEmail(e.target.value)} type="email" className="mb-3 w-full rounded-xl bg-slate-100 p-4" placeholder="Email" /><input required value={password} onChange={(e) => setPassword(e.target.value)} type="password" className="w-full rounded-xl bg-slate-100 p-4" placeholder="Password" /><button disabled={loading} className="mt-5 w-full rounded-xl bg-gradient-to-r from-violet to-coral p-4 font-bold text-white disabled:opacity-60">{loading ? 'กำลังเข้าสู่ระบบ...' : 'Log in'}</button>{error && <p className="mt-3 text-red-600">{error}</p>}<p className="mt-5 text-sm text-slate-500">Demo: demo@cristal.page / demo123</p></form></main>;
}
