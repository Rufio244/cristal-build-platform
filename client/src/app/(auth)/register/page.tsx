'use client';
import { FormEvent, useState } from 'react';
const API = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:5000';
export default function RegisterPage() { const [name, setName] = useState(''); const [email, setEmail] = useState(''); const [password, setPassword] = useState(''); const [error, setError] = useState('');
  async function submit(event: FormEvent) { event.preventDefault(); setError(''); try { const response = await fetch(`${API}/api/auth/register`, { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ name, email, password }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error || 'สมัครสมาชิกไม่สำเร็จ'); localStorage.setItem('cristalToken', data.token); localStorage.setItem('cristalUserId', data.user.id); localStorage.setItem('cristalUser', JSON.stringify(data.user)); window.location.href = '/dashboard'; } catch (err) { setError(err instanceof Error ? err.message : 'สมัครสมาชิกไม่สำเร็จ'); } }
  return <main className="flex min-h-screen items-center justify-center bg-[#edf1fa] p-6"><form onSubmit={submit} className="w-full max-w-md rounded-3xl bg-white p-8 shadow-soft"><h1 className="text-3xl font-black text-violet">Create your account</h1><p className="mb-6 mt-2 text-slate-500">Start building with AI today</p><input required value={name} onChange={(e) => setName(e.target.value)} className="mb-3 w-full rounded-xl bg-slate-100 p-4" placeholder="Full name" /><input required value={email} onChange={(e) => setEmail(e.target.value)} type="email" className="mb-3 w-full rounded-xl bg-slate-100 p-4" placeholder="Email" /><input required minLength={6} value={password} onChange={(e) => setPassword(e.target.value)} type="password" className="mb-3 w-full rounded-xl bg-slate-100 p-4" placeholder="Password (6+ characters)" /><button className="mt-3 w-full rounded-xl bg-gradient-to-r from-violet to-coral p-4 font-bold text-white">Create account</button>{error && <p className="mt-3 text-red-600">{error}</p>}</form></main>; }
